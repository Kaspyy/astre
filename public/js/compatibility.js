const overallCompatibilityPercentage = document.querySelector(".overall-percentage-foreground-bar");
const loveCompatibilityPercentage = document.querySelector(".love-percentage-foreground-bar");
const sexCompatibilityPercentage = document.querySelector(".sex-percentage-foreground-bar");
const friendshipCompatibilityPercentage = document.querySelector(".friendship-percentage-foreground-bar");

let userZodiacSign = document.querySelector(".sign-icon").textContent.trim();
let otherZodiacSign = document.querySelector(".their-zodiac-sign").textContent.trim();

console.log(otherZodiacSign)